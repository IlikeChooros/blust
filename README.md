# blust

A basic implementation of the deep learning models in c++.


### Compilation

Optimized build (for debug simply change `Release` to `Debug`):

```bash
mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
make
```